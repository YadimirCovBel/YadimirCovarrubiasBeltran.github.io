const fs=require("fs"),path=require("path"),source=path.join(__dirname,"docs",".nojekyll"),destinationDir=path.join(__dirname,"docs","browser"),destination=path.join(destinationDir,".nojekyll");fs.existsSync(destinationDir)||fs.mkdirSync(destinationDir,{recursive:!0}),fs.copyFile(source,destination,o=>{o?console.error("Error copying .nojekyll file:",o):console.log(".nojekyll file successfully copied to docs/browser")});